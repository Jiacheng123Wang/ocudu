# GPU PHY 融合车道时延 —— **高层现状与计划**

> **本文件只放高层**：一句话现状、验收判据、阶段进度、下一步、未决与待裁决。
> 细节（读数与证据、机制与归属、仪表手册、跑腿规范、每条改动的实施记录、坑与教训）
> 一律在 **`gpu_phy_latency_optimization_design_and_implementation.md`**（下称"开发文档"）；
> 会话快照在 `session_handoff_*.md`。三类文档的分工见 `README.md`。
>
> **活文档**（可重写）。最近更新：**2026-09-25**（Q9 修复落地 + 离线验证；确认腿待用户跑，见 §3.2）。

---

## 0. 一句话现状

`gpu` 融合车道的端到端时延（重上行、中位 **≈2675 µs**）**每一项都已归位**：
约 473 µs 等本槽样点 + 48 µs 前端主机 + **901 µs 等单车道空出来** + **1125 µs 本跳设备执行** + 110 µs Pass-3；
而接收缓冲的**持有期 = 这个跨度**，池只有 **8 个 × 每槽 1 个** ⇒ **≈4 ms 就把池抽干**，
接收线程被 `pop_blocking()` 阻塞、电台 underflow（`gpu` 476–733 次 vs 同负载 `cpu` 0–1 次，3/3 条腿复现）。

**进度**：P0 仪表**全部完成并且已在空口收口**（P0-6 生效并发度 / P0-1 分组 GPU 时间 / **P0-5 探针配对**——
腿 `p05-pair` 上配对**精确相等**：73529 == 73529，零丢失；`p0_gate.sh` 10/10 PASS）；
P1-8（车道并发度 1→2）**已实测并两次复现**：`ce` 3228 → ~51 µs、跨度 −55%，
**但代价是同一改动压出一次 ~5 秒收包停顿（两次复现）⇒ 只能算测量结论，不能交付**；
**Q9 的根因已结案、修复已落地（开发文档 §6.10/§6.11，提交 `3d00eafe97`），确认腿 `p08-conc2` 已飞（§6.12）**：
修复**确实把注册表那一侧治好了**（`starved_events` **383 → 6**、`oldest unclaimed age max` **77.9 s → 5.95 s**、
不再有 10.24 s 整数倍的等待、`keepalives` 零泄漏、`late_time=15` 说明新触发在工作），
**但 UL 断流没有消失**——剩下的持有者是**命令缓冲的完成**：7–8 个块在 **~3 ms** 内被认领（注册表这一侧已经好了），
它们的 `deposit->completion ≈ 5.00 s`，而输入 token 是在 **cb 的完成处理器**里回池的 ⇒ 池被按住 8/8 ⇒ park 4.997 s ×2
⇒ RT `late`/underflow ⇒ 3 次 RX overflow（= 全部 9.98 s 样点丢失）⇒ 调度器 `All the UE HARQs are busy waiting for their results`
⇒ 不发 grant ⇒ UE 掉线重接 ⇒ iperf3 断流。**A + B 已落地（§6.13，提交 `5ff8759804`，用户批准）**：A = 干池的接收线程自己驱动 sweep（补上「全停时没人来问」的洞），B = 两组新读数（注册表 `registry commit->completion=`、lane 的 `commit -> completion (Q9-B)` 与最慢 cb 表）⇒ **⏳ 等一条腿**（判据仍是 F1–F8，没有新阈值）。**C（结构性，前端 DFT 单独一次提交）仍等用户裁决**。
**P2-E（输入缓冲寿命解耦）机制已实现，但被平台证伪**（macOS 26.6.2 只在命令缓冲完成时发布"中途"事件信号），
**待用户裁决**（§5）。**P2-F（并发度作为交付）也必须用户二次裁决**（Q9 既已修复，它的前置条件已清）。

⚠ **P0-5 的配对读数同时作废了两个被当作"事实"的比例**（开发文档 §6.3 ⑥）：
"residency 里 ~95% 是 busy" 只对 **n78 加压腿**成立（0.93），**n1 默认腿是 0.643**；
"`eq_demap` ≈ residency" 在两条腿上都**不是**恒等式（都 ≈ **1.09 ×** residency）。
⇒ 引用 D 项（本跳设备执行）时必须写清**哪条腿的 busy**：n1 的一跳设备执行是 **517 µs（中位）**，不是 1125。

---

## 1. 目标与验收判据（**先写死，不随结果改**）

| # | 判据 | 阈值 |
|---|---|---|
| **V1** | `[ul_gpu_pipeline]` 中位跨度 | **≤ 2150 µs**（重载基线 2675 µs，−20%）|
| **V2** | 症状消失：接收池 | `starved_events == 0` 且 `held_max < pool`（池 = 8）|
| **V3** | 电台 | RF 失败 **≤ 10**（cpu 量级 0–1）、`gaps == 0` |
| **V4** | **不许用提交数换时延** | `cbs/lane ≤ 2.00 (max=2)`、`dropped == 0`（用户裁决：**只约束交付**，测量臂放行）|
| **V5** | 不回归 | 契约 8/8、`crossings 0.00+0.00`/跳、CRC KO% 不劣化 |

**验收腿 = 加压配方**（n78，`iperf3 -c <phone> -R -b 40M -P 4 -t 240`，`--regime=stress`）。
**默认工况**（n1 + 接入 + 标准流量）判契约 8/8 与 `stale=0`。两种配方的完整写法、以及
"判据有三个绑定维度（工况 / 流量 / 几何）"的纪律见开发文档 §3.2/§3.3/§5。

---

## 2. 阶段与进度

| 阶段 | 内容 | 状态 |
|---|---|---|
| **P0** | 仪表补齐（P0-6 并发度、P0-1 分组 GPU 时间、P0-5 探针配对、**P0-2 持有期直方图 + 池等待时长**、**P0-7 逐块生命周期**）| ✅ **全部完成**：P0-5 空口已收口（`p05-pair`，10/10）；P0-2 已落地（§6.7）、P0-7 已落地（§6.9），`pop_blocking wait` / `input hold` / `block lifecycle` 三行随腿可读 |
| **P1** | 单变量臂（P1-1/2/3 已由读码收口；**P1-8 已跑**；P1-4/5/6/7 待跑）| 进行中 |
| **P2** | 结构/实现改动（P2-A/B/D/E/F）| P2-E 已实现但被平台证伪；其余未开工 |
| **缺陷修复** | **Q9（sweep 的 slot 比较跨环绕失效）** | ✅ 已实现 + 离线全绿（§6.11，`3d00eafe97`）；确认腿 `p08-conc2`（**§6.12**）：注册表侧治好（`starved_events` 383→6），**UL 断流仍在** ⇒ 下一个持有者 = **cb 完成 5 s**。**A（干池驱动 sweep）+ B（两组新读数）已落地（§6.13，`5ff8759804`）**，等腿；**C 等裁决** |
| **P3** | 验收 V1–V5 | 未开始 |

**已测到的两个杠杆**：
① **P1-8 车道并发度**（1→2）：`ce` −58~64×、跨度 −55%（两次复现），**代价**是 ~5 秒收包停顿（两次复现）；
② **D 项本跳设备执行**（1125 µs）：P0-1 已给出 `dft` vs 其余的分组读数（空口：`dft=937.9 µs/lane (88%)`），
但**本机不支持逐 dispatch 计数器** ⇒ kernel 级还得靠 `MTLCounterSampleBuffer` 或继续分片。

---

## 3. 下一步

### 3.1 等用户裁决的（我无法单方面决定）

1. **P2-E 怎么走**（开发文档 §6.4.4 的选项表）：建议**保留开关（默认关）作仪器**，然后二选一——
   **接受 +1 提交**把前端块做成独立提交（V4 需重新裁决），或转 **P2-D**（用 P0-5 的配对分布按持有期定池容量）。
2. **P2-F（并发度作为交付）**：§7.5 的杠杆已实测，且 **Q9 的修复已落地** ⇒ 前置条件已清，
   等确认腿（§3.2 第 1 条）绿了就能重新裁"并发 2 是否可交付"。
3. **两条陈旧网**（§4 Q11）：`value_net` 的归档基线是否重建；`ab_dumps` arm1 是否曾经绿过。

### 3.2 可以立刻做的（不需要裁决）

1. **✅ 确认腿已飞（`p08-conc2`，§6.12）**：F1–F5 未过，但机制已经换人——**注册表侧治好了**（`starved_events` 383→6），
   剩下的持有者是**命令缓冲的完成**（认领 ~3 ms，`deposit->completion ≈ 5.00 s`，token 在完成处理器里回池）。
2. **✅ A + B 已落地（§6.13，提交 `5ff8759804`）**：A = 干池的接收线程自己驱动 sweep；B = `registry commit->completion=`
   与 lane 的 `commit -> completion (Q9-B)` ＋ 最慢 cb 表（外加现成 `OCUDU_CE_WAIT_TRACE=1` 看 held cb 由谁、隔多久才 close）。
   **⏳ 下一步：飞一条与 `p08-conc2` 同配方的腿**（§6.13 ④ 的表说清"要读什么、每个读数对应哪条支路"；判据仍是 F1–F8）。
3. **C（结构性，需裁决）**：前端 DFT 单独一次提交（= P2-E 选项 (b)）——若 B 的表指向"设备侧等待 / hold 跨跳"，就是它。
   —— 以下为已完成的历史读数：P0-7（§6.9）给出了"谁按住了输入"的直接读数，`p07-conc2` 上 `input hold` max =
   `deposit->completion` max = 30.72 s ⇒ 结论见 §6.10。
3. **P1-6**（`OCUDU_UL_SLOT_TRACE` 逐槽时间线）把 B 项（48 µs）拆开，并补 `ce` 尾部的残余归因。
4. **P2-D 的输入已经齐了**：P0-5 的配对分布给出了 n1 的真实持有期分母
   （`residency` 835.5 / `busy` 515.9，配对样本 73529）——如果 P2-E 走不通，这是最便宜的一条。
5. ~~下一次动代码时的两件小事（配对账输出 + 访问器）~~ ✅ **已随 P0-2 一起做完**（开发文档 §6.7）。

**P0-5 的腿已经飞过（`p05-pair`）**，无需再飞：`p0_gate.sh p05-pair` → **10/10 PASS**，读数见开发文档 §6.3 ⑥。

---

## 4. 未决问题（状态一览；细节与出处见开发文档 §8）

| # | 问题 | 状态 |
|---|---|---|
| Q1 | `merged_hop` 内各 kernel 各占多少 | **开放**（头号仪表缺口；本机不支持逐 dispatch 计数器）|
| Q2 | `ce` 的 p95 尾巴（1782 µs）来源 | 基本收口（单车道排队 + 上一跳缓冲回收）；两者各占多少待 P1-6 |
| Q3 | `t2f` 521 µs 是 DFT 执行还是宿主？ | ✅ 两个都不是：473 收样点等待 + 48 宿主 ⇒ `OCUDU_DFT_*` 在此段无杠杆 |
| Q4 | 三段是否重叠？ | ✅ 同跳内是墙钟划分；杠杆是**跨跳并发** |
| Q5 | residency 里的 busy 是必要工作还是低效执行？ | 开放，依赖 Q1。⚠ **"~95% busy" 已作废**：n1 配对后 0.643、n78 0.93，逐腿读 |
| Q6 | 并发度能否吃掉 `ce` 的排队项？代价？ | ✅ 能（−58~64×）；代价 = 5 秒停顿（两次复现）|
| Q7 | 符号级收包在负载下对跨度的效果 | 开放（必须在加压腿 + 融合路径上重量）|
| Q8 | n78/n1 的生效并发度 | ✅ 都是 **1**（串行 strand）；上限 = 池值 5 |
| **Q9** | **并发 2 下的 UL 断流 / 收包停顿** | ✅ **已结案 + 已修复**（开发文档 §6.10 结案 / **§6.11 修复**，提交 `3d00eafe97`）：根因是**代码缺陷**——注册表 sweep 的 `entry.slot + 2 < slot` 用的是**模 10240 的 `slot_point::count()`**，跨 hyperframe（10.24 s）失效 ⇒ 落在环绕末尾的无人认领块等**一整个周期**（实测 = 10.24 s 的整数倍）甚至永远，其间按住输入 token ⇒ 池空 ⇒ 接收线程 park（实测 4.998 s ×2）⇒ 电台丢 9.97 s 样点 ⇒ UL 归零。修复 = **slot 窗口（环绕安全守护）+ 10 ms 单调时钟期限**、**每个注册表入口都查**；离线全绿。**确认腿 `p08-conc2`（§6.12）：修复生效但 UL 断流仍在**——剩下的持有者是 **cb 完成 ~5.00 s**（token 在完成处理器里回池）⇒ 待做 A/B/C |
| **Q16** | 那个"没人认领的块"到底等了多久？ | ✅ 已答（§6.10）：最久 **30.72 s = 3 × 10.24 s**，救场者是 sweep；P0-7 的 `block lifecycle` 三行是直接读数（§6.9）。修复后 `unclaimed at once max` / `oldest unclaimed age max` 应落 ms 级（确认腿判据 F2）|
| **Q15** | 并发 2 的收益来自重叠还是在飞缓冲变多？ | ⚠ 初读（§6.8 ②）：`busy max = 1.48 ms` vs `residency max = 5.004 s`、`carried=986` ⇒ 尾巴是**缓冲之间的间隔**；`merged_hop` busy 888.8 µs/lane（并发 1：540）⇒ **收益=重叠，代价=等待链变长** |
| Q10 | n1 2.85 倍退化是否还有 `ce` 之外的成分 | 主因已定位；`p05-pair` 配对后 `ce` = 3278 µs = 跨度的 **62%**，设备执行只有 517 µs |
| Q11 | 两条"改前就红"的网怎么处理 | **待用户裁决** |
| Q12 | `s84b-p0` 第一次尝试没有日志（最可能被"戳 ≠ HEAD"拒绝）| 登记（低优先，**未验证**）：要它作证据就得重飞；否则按无效腿处理 |
| Q13 | 配对账里 **199 行（0.2%）** 既没匹配也没淘汰（被同 slot 的后一条车道覆盖）| 低优先；与 Q14 的自解释输出一起做 |
| Q14 | 一跳的设备执行在 n1 上是多少？ | ✅ **已收口（P0-5 配对）**：**`busy` = 517 µs（中位）**，不是 residency 836、也不是 n78 的 1125 |

### n1 默认腿 2.85 倍退化（已定位，2026-09-24/25）

| 腿 | 小区 | `[ul_pipeline]` 中位 | 池 `starved_takes` |
|---|---|---|---|
| `s47/s49/s51`、`s61/s62` | n1（`origin/main` 合并前）| **1844–1896 µs** | 1–39 |
| `s83-tag` / `s84-p0` | **n1（合并后）** | **5263 / 5260 µs** | 696 / 1192 |

单变量腿定死主因：**并发 1 时 `ce` = 3228 µs；并发 2 时 `ce` = ~51 µs、跨度 5268 → 2400/2359**（两次复现）
⇒ **是"等串行 strand"，不是 GPU 算力、不是收样点**。⚠ 但同一条改动也两次复现了那次 **~5 秒收包停顿**（Q9）。

---

## 5. 需要用户裁决的事项（汇总）

| # | 事项 | 选项 / 我的建议 |
|---|---|---|
| 1 | **P2-E**（机制被平台证伪）| ✅ **已裁决（2026-09-25）：保留开关作仪器（默认关）**；不做 +1 提交/第三次提交/宿主拷贝/自旋轮询，也不因此转 P2-D。**为 P2-E 不需要再飞腿**（开发文档 §6.4.6）|
| 2 | **P2-F**（并发度作为交付）| §7.4 的裁决只放行测量臂；要交付必须再裁一次。**前置条件（查清 Q9）已清**：Q9 已修复（§6.11）⇒ 等确认腿绿了就能裁 |
| 3 | **两条陈旧网** | `value_net` 重建归档基线（= 承认过期）还是标为 HEAD 不可用；`ab_dumps` arm1 查清是否曾绿 |
| 4 | ~~P0-5 的空口腿~~ | ✅ **已飞（`p05-pair`）**：配对精确相等、门 10/10（开发文档 §6.3 ⑥）|
| 5 | ~~Q9 确认腿~~ | ✅ **已飞（`p08-conc2`，§6.12）**：修复生效、症状未消失；后续见下一条 |
| 6 | **P2-E 选项 (b) 是否重新裁决**（前端 DFT 单独一次提交）| 用户 2026-09-25 曾裁「保留开关作仪器、不做 +1 提交」；**§6.12 ③ 的新证据**：交接块的输入被"跨跳未提交的 cb"按住 5 秒是**池被抽干**的机制 ⇒ 建议**重新考虑 (b)**（代价：V4 `cbs/lane` 需重裁）。**A+B 已按用户指示先做（§6.13）**；等 §6.13 ④ 的腿读数出来再裁最快 |

---

## 6. 与里程碑的关系 / 不做的事

* 本工作流**不是**"契约变红"：契约 8/8、crossings `0.00+0.00`、就地装配在**所有重载腿上仍成立**；
  它针对的是"**车道在高负载下给电台添麻烦**"——里程碑之后的新工作项。
* **不做**：用额外提交换时延（V4）；用更大的池冒充时延改善（那是诊断 P1-4）；改预登记判据去迁就某条腿。

---

## 7. 文档索引（`doc_chinese/phy_latency/`）

| 类 | 文件 | 说明 |
|---|---|---|
| design & implementation | `gpu_phy_latency_optimization_design_and_implementation.md` | 现象/机制/仪表/跑腿规范 + **追加式实施记录**（6.1 P0-6、6.2 P0-1、6.3 P0-5、6.4 P2-E、6.5 缺陷、6.6–6.8 Q9 追查、6.9 P0-7、6.10 Q9 结案、6.11 Q9 修复、6.12 确认腿、**6.13 Q9-A/Q9-B**）+ 杠杆与候选 + 未决 |
| session handoff memo | `session_handoff_2026-09-24-1.md` | 上一会话交接快照（当时任务：先 P0-5 再 P2-E）。**会话类只保留快照**；2026-09-25 那一份的内容已按用户要求并入开发文档（§5.3/§6.3/§6.4/§8），文件已删除 |
| high level status and plan | **`high_level_status_and_plan.md`** | 本文件 |
| 目录约定 | `README.md` | 三类文档的分工、文件命名、引用规范、旧名映射 |

**上游（历史）记录**：`doc_chinese/phy_pipeline_gpu/gpu_phy_pipeline_design_and_implementation.md`（**追加式，不改历史**）。
**腿日志**：`doc_chinese/phy_pipeline_gpu/wip/logs/`；**门与工具**：`doc_chinese/phy_pipeline_gpu/wip/`。

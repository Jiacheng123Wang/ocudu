# `doc_chinese/phy_latency/` —— GPU PHY 流水线**时延**工作流的目录

> 本目录于 **2026-09-24** 按用户要求建立：**此后与本项有关的文档、计划、memo、执行记录都放这里**。
> 上游（历史）记录仍然是 `doc_chinese/phy_pipeline_gpu/gpu_phy_pipeline_design_and_implementation.md`（**追加式**，不改历史），
> 本目录只放**活文档**：结论会随测量更新，所以它们**可以被重写**（与主设计文档的规矩相反，特此写明）。

## 为什么有这个工作流（一句话）

重上行空口腿（2026-09-24）量到：**`gpu` 融合车道在 ~14–19 Mbit/s 就把 8 个接收缓冲抽干** ⇒ `pop_blocking()` 阻塞接收线程 ⇒
电台 underflow 几百次；而**同样负载下 `cpu` 模式 0–1 次**。用户裁定：**加大池是治标**，
根因是**数据在 GPU 流水线里停留太久**（接收缓冲的持有期 = 流水线的端到端时延）。
⇒ 本工作流的目标：**缩短 `[ul_gpu_pipeline]` 的端到端时延**，并以"池不再饥饿"为症状消失的判据。

## 文件约定

| 文件 | 作用 | 是否可重写 |
|---|---|---|
| `README.md` | 本文件：用途、约定、索引 | 是 |
| `00_status.md` | **现状**：测到什么、机制、证据、未决问题 | 是（每次有新读数就更新）|
| `01_plan.md` | **详细优化计划**：阶段、假设、改动点、判据、成本、依赖 | 是 |
| `02_measurement.md` | **仪表与读法**：每个读数怎么来的、怎么复跑、哪些坑 | 是 |
| `memo_YYYY-MM-DD_<slug>.md` | 一次性的分析/结论/裁定 | 否（写完只追加更正）|
| `exec_YYYY-MM-DD_<slug>.md` | 一次执行的记录（改了什么、跑了哪条腿、读数、结论）| 否（只追加）|
| `logs/` | 本工作流自己的证据归档（脚本输出等；**腿日志仍在 `phy_pipeline_gpu/wip/logs/`**）| 否 |

**引用外部时一律用"章节名/腿名"而不是行号**（合并 `origin/main` 之后行号已整体漂移过一次，见主文档 §5.9.123）。

## 索引

* `00_status.md` —— 当前读数与机制（**先读这个**）
* `01_plan.md` —— 优化计划（分阶段、每条带判据）
* `02_measurement.md` —— 仪表手册（三段分段、车道、池、怎么复跑）

## 与里程碑的关系（不要搞混）

融合车道的**契约性质**（8/8、`0.00+0.00`/跳、就地装配、0 wraps）在**所有重上行腿上都成立**；
**不成立的是"车道在高负载下不给电台添麻烦"**。所以本工作流是**里程碑之后的新工作项**，
它不是"契约变红"，而是"车道在高负载下的时延把接收侧拖垮"。
* `03_p0_instrumentation.md` —— **P0 仪表**：P0-6（车道生效并发度）与 P0-1（诊断拆分 `OCUDU_LANE_DIAG_SPLIT`）的读数、读法、待补的那一次空口读数与判据

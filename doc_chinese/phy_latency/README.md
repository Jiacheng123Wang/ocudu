# `doc_chinese/phy_latency/` —— GPU PHY 融合车道**时延**工作流

> 本目录于 **2026-09-24** 按用户要求建立：**此后与本项有关的文档都放这里**。
> 上游（历史）记录仍是 `doc_chinese/phy_pipeline_gpu/gpu_phy_pipeline_design_and_implementation.md`
> （**追加式，不改历史**）；腿日志在 `doc_chinese/phy_pipeline_gpu/wip/logs/`，腿脚本与门在 `doc_chinese/phy_pipeline_gpu/wip/`。

## 为什么有这个工作流（一句话）

重上行空口腿（2026-09-24）量到：**`gpu` 融合车道在 ~14–19 Mbit/s 就把 8 个接收缓冲抽干** ⇒ `pop_blocking()` 阻塞接收线程 ⇒
电台 underflow 几百次；而**同样负载下 `cpu` 模式 0–1 次**。用户裁定：**加大池是治标**，
根因是**数据在 GPU 流水线里停留太久**（接收缓冲的持有期 = 流水线的端到端时延）。
⇒ 目标：**缩短 `[ul_gpu_pipeline]` 的端到端时延**，并以"池不再饥饿"为症状消失的判据。

## 文档分三类（**2026-09-25 起**）

| 类 | 文件 | 性质 | 读者 |
|---|---|---|---|
| **design & implementation** | **`gpu_phy_latency_optimization_design_and_implementation.md`** | **开发文档**：现象与证据、机制与归属、仪表手册、跑腿规范、**追加式实施记录**（每条改动的设计与验证）、杠杆与候选、未决问题。§1–§5 活（可重写，但更正必须留痕）；**§6 起只追加、不改历史** | 开发本项的人 |
| **session handoff memo** | `session_handoff_<会话开始日>-<序号>.md` | **状况快照**：给**新会话**开工用的一份 memo（现状、读数出处、下一步、仍挂着的事）。写完只追加更正。⚠ **memo 只是快照，不是记录**：一条会话的**结论与证据要并进开发文档**（见下表）| 新会话的第一个读者 |
| **high level status and plan** | **`high_level_status_and_plan.md`** | **高层**：一句话现状、V1–V5 判据、阶段进度、下一步、未决与**待用户裁决**。活文档，可重写 | 决策者 / 快速对齐 |

**规矩**

* **交接 memo 的命名**（用户 2026-09-25 明确）：`session_handoff_<日期>-<序号>.md`，**日期取会话开始那天**（跨午夜不改），
  序号是那天的第几份。⇒ 2026-09-24 开始的这条会话，它的第一份是 `session_handoff_2026-09-24-1.md`、
  **它的交接是 `session_handoff_2026-09-24-2.md`**；`session_handoff_2026-09-25-1.md` 是**下一个会话**的第一份（现在不该存在）。
* **细节只写进开发文档**（读数怎么来的、哪条腿、哪个提交、离线怎么验）；高层文档与 memo **只引用，不复制**细节。
* 新的会话记录写成**新的** `session_handoff_*.md`，**旧的不改**（除指向改名/迁移的指针）。
* 引用外部（主设计文档等）**一律用"章节名/腿名"，不用行号**（行号在合并后漂移过一次，见主文档 §5.9.123）。
* 本目录的**活文档可以被重写**（与主设计文档的"只追加"相反，特此写明）；开发文档的 §6 起是追加式。

## 旧文件名 → 现在在哪（2026-09-25 整理）

| 旧文件 | 现在的位置 |
|---|---|
| `00_status.md` | 现象/机制/设备读数 → 开发文档 **§2**；判据与配方 → **§3**；未决问题 → **§8**；高层一览 → `high_level_status_and_plan.md` |
| `01_plan.md` | 判据/负载配方 → 开发文档 **§3**；归属式预算与 P1/P2 候选 → **§7**；阶段进度与下一步 → `high_level_status_and_plan.md` |
| `02_measurement.md` | 仪表手册 → 开发文档 **§4**；跑腿与门的规范 → **§5** |
| `03_p0_instrumentation.md` | P0-6 → 开发文档 **§6.1**；P0-1 → **§6.2**；流程教训 → **§6.5** |
| `04_p2e_plan.md` | P2-E 的计划原文摘要 → 开发文档 **§6.4.1** |
| `05_p2e_platform_finding.md` | P2-E 的实现与**平台证伪** → 开发文档 **§6.4** |
| `session_handoff_2026-09-25-1.md`（**一次命名错误**：它写于 2026-09-24 那条会话里，按命名规则应叫 `-09-24-2`；用户指出后其内容并入开发文档、文件删除，该文件名留给下一个会话）| 仓库/构建状态 → 开发文档 **§5.3**；P0-5 → **§6.3**；P2-E 平台证据 → **§6.4.3**；两句"改前就红"的网 → **§6.3④/§6.5⑤/§8 Q11**；`s84b-p0` 无日志 → **§8 Q12**；下一步 → `high_level_status_and_plan.md` **§3** |

（这些旧文件的内容已**整体并入**开发文档，旧文件已删除；git 历史里仍可查到。）

## 索引

* **`gpu_phy_latency_optimization_design_and_implementation.md`** —— 开发文档（**先读这个**）
  * §2 现象与基线读数 · §3 判据与负载配方 · §4 仪表手册 · §5 跑腿与门
  * §6 实施记录（6.1 P0-6 · 6.2 P0-1 · 6.3 P0-5 · 6.4 P2-E · 6.5 缺陷与教训）
  * §7 杠杆与候选 · §8 未决问题 · §9 证据索引（腿 → 用途）
* `high_level_status_and_plan.md` —— 高层现状与计划（决策/对齐用）
* **`session_handoff_2026-09-25-3.md`** —— **最新会话交接快照（新会话开工先读这一份）**：断流结案（Metal 5.00 s 等待上界 + 修复 A/B 空口验证）/ **V1 仍未达（−9%，2428→2440 µs）** / **§6.29 方向更正：941 µs 是占用窗口不是 DFT 算力** / 下一步 = 前端 14 次派发合成 1 次（离线验收 + 腿 `p22`）/ 纪律 / 文件地图 / 开工第一句话
* `session_handoff_2026-09-25-2.md` —— 上一份交接（Q9-F/Q9-F2/Q9-F3 仪器就位、腿 `p14-conc2` 待飞）；历史快照，只作参考
* `session_handoff_2026-09-25-1.md` —— 更早的交接（p13 之后：只剩"队列次序"）；历史快照，只作参考
* `session_handoff_2026-09-24-2.md` —— 更早的交接（Q9 结案前）；历史快照，只作参考
* `session_handoff_2026-09-24-1.md` —— 同一天的第一份快照（上一会话；读完它请接着读 `-09-24-2`）

## 本阶段的工具（`wip/`，2026-09-25 起）

* **`wip/p0_gate.sh`** —— P0 读数门（只读日志，用户飞腿时可安全跑）：A1/A2（P0-6）、B1/B2（P0-1 拆分臂）、C1/**C2/C2b**（P0-5 配对）、
  **D1–D4（判据：Q9 修复确认 F1–F4，阈值见开发文档 §6.11 ⑤）＋ D5–D12（INFO 读数：sweep 触发器、Q9-A/B、Q9-C、Q9-D 栅栏次序、
  Q9-E 处理器滞后、**Q9-F 提交次序、Q9-F2/F3 前端块与队列占用**，见 §6.13–§6.19）＋ D13–D15（INFO 读数：
  **提交握手计数（§6.20/§6.21）、按需 dump（§6.24）、修复 B 的 `dropped=`（§6.26）**）**；自测见 **`wip/p0_gate_selftest.sh`**。
* **`wip/leg_census.py`** —— 腿普查：UL 静默表 + RF/pool 事件（定位"UL 在哪一段停住"）。
* **`wip/metal_wait_timeout_probe.mm`** —— 离线复现 Metal 对"未被满足的设备侧事件等待"的 **5.00 s 上界**（§6.20）。
* **`wip/dft_kernel_cost.mm`** —— 离线 DFT 微基准（**加载生产 `ocudu_dft.metallib`**）：派发形状 ↔ 开销；
  `xforms=14` 行（一个 n78 时隙的 14 个符号）是**前端批量化改造的验收判据**（§6.29）。
* **上一阶段的门与工具仍在 `doc_chinese/phy_pipeline_gpu/wip/`**（起腿 `run_leg.sh`、`milestone_audit.sh`、离线网、**腿日志 `logs/`**）；
  跑出来要看的东西在 **`doc_chinese/work_tmp/`（git 忽略）**——`/tmp` 会在重启时被清掉。

## 与里程碑的关系（不要搞混）

融合车道的**契约性质**（8/8、`0.00+0.00`/跳、就地装配、0 wraps）在**所有重上行腿上都成立**；
**不成立的是"车道在高负载下不给电台添麻烦"**。所以本工作流是**里程碑之后的新工作项**，
它不是"契约变红"，而是"车道在高负载下的时延把接收侧拖垮"。

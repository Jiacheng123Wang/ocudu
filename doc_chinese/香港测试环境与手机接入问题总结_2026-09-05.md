# 香港测试环境与手机接入问题总结（2026-09-05）

## 1. 背景

OCUDU gNB（srsRAN fork，apple-silicon 分支）+ open5GS 核心网的 5G SA 测试系统从北京搬到香港后：
- gNB/核心网侧很快恢复（改 IP 后 NG Setup 成功）
- OAI UE（SDR 软件 UE）全程接入无问题
- 但两台手机（OnePlus 8T、iPhone 17 国行）在香港均无法接入，且行为与在北京时完全不同

调查结论：**问题不在 gNB、核心网、SIM 卡或频率，而在手机 modem 的区域策略门控（手机"知道自己身处何地"，并按区域开关 5G SA 搜索/接入）**。

## 2. 系统拓扑（香港）

| 机器 | 系统 | 角色 | B210 | 备注 |
|---|---|---|---|---|
| Mac（192.168.31.121/204） | macOS | gNB（apple-silicon 分支） | 000000560 | 经 ThinkPad 扩展坞以太网（en11，.204）出网 |
| jwang-ASUS（192.168.31.107） | Ubuntu 24.04 | gNB（对照平台） | GPSDO 板 | gcc-13 编译，`build/apps/gnb/gnb` |
| jwang-5GC（192.168.31.211） | Ubuntu 24.04 | OAI UE / 旧 srsRAN 构建 | 2604127 | `nr-uesoftmodem` 已编译通过 |
| vagrant@192.168.31.15 | Ubuntu VM | open5GS 核心网（HK 初期） | — | 已停用（备用） |
| jwang@192.168.31.122 | Ubuntu VM（UTM，Ubuntu2204Destop） | **open5GS 核心网（现用，北京/广州历史日志在此）** | — | AMF NGAP 双地址监听 192.168.64.4 + 192.168.31.122:38412；登录 `ssh jwang@192.168.31.122` |

- B210 共 2 块，会随测试在机器间移动。
- 核心网已固定到 macOS UTM 虚拟机（192.168.31.122）：历史日志（01/13–09/03 北京/广州）与 HK 日志统一归档。Mac gNB 经 UTM 桥（bridge100，192.168.64.1→64.4）直达核心网。
- **soft UE 专用 IMSI 已改为 001019876543210**（OAI UE 配置），与 iPhone 的 001010123456789 彻底区分，避免核心网日志混淆。

## 3. 香港 n78 扫频结果（Mac B210 RX2，增益 70 dB）

3.3–3.8 GHz 整体较干净，商用载波只有 **−89 ~ −94 dBm**（弱），分布：

- 3313.75–3359.25（~45 MHz，HK 3.3G 频段多载波）
- 3404.75–3408.25 / 3429.75–3438.25 / 3463–3494（多段）/ 3499.75–3509.25 / 3523.75–3540.25 / 3583.75–3599.25
- **3600 MHz 以上完全干净**

推荐频点（已写入配置注释）：
- `625000` = 3375.0 MHz（3359/3405 之间空隙，3500 以下最干净）
- `625920` = 3388.8（原 iPhone 验证点）/ `630000` = 3450.0（3438–3464 空隙）
- `641000`/`645333`/`651000`（3615/3680/3765，3600 以上干净区，但 iPhone 对 >3500 MHz 无反应）
- 旧点 `632628`=3489.42 在香港与 3480.75–3494.25 商用载波簇重叠，`627264`=3408.96 下缘有弱载波重叠

配置产物：
- `configs/gnb_rf_b200_tdd_n78_20mhz_HK.yml`（Mac 用，AMF 31.15，绑定 31.204，625000）
- `configs/gnb_rf_b200_tdd_n78_20mhz_HK_107.yml`（107 用，绑定 31.107）
- `configs/gnb_uhd_oaiue.yaml`（107 在用，632628、GPSDO、DDDSU、tx_gain 85 无回退——为 OAI UE 调优过）
- `configs/gnb_uhd_oaiue_3450.yaml`（3450 变体，备用）

## 4. 平台对比：Ubuntu gNB vs macOS gNB

### Ubuntu gNB（107）——健康
- 实时性故障仅 1 次
- OAI UE 接入全程成功：注册完成 + PDU 会话 + ping 通核心网 10.45.0.1

### macOS gNB（Mac）——曾因机器过载"假坏"
- 最初 2628 次实时性故障（`late DL_TTI.request` / `PRACH request late` / RF underflow），TX 发不出有效 SSB，手机和 211 扫描都看不到小区
- 根因：**Mac 负载 18（14 核）**——OneDrive 同步 137% + fileproviderd 113% + Visual Studio 100% + suggestd 100% + Spotlight 索引 ~81%
- 清理负载后：故障降到 116 次，OAI UE 成功接入并拿到 10.45.0.3

结论：macOS gNB 平台本身可用，但**对系统负载极敏感**（20 MHz TDD 23.04 MS/s 实时要求高），跑 gNB 前必须清退 OneDrive/VS/Spotlight 等重负载进程。

## 5. 手机谜团时间线

### iPhone 17（国行，iOS 随系统更新）
| 时期 | 现象 |
|---|---|
| 2 月（srsRAN_Project gNB） | **完整接入** |
| 现在（OCUDU gNB，n1） | RACH→RRC→注册请求→收到 NAS 认证请求后 UL 功率骤降（DMRS EPRE −7dB 后消失），**死在认证一步** |
| 现在（老 srsRAN_Project gNB） | **连注册请求都没有**（无任何 log 输出）——北京、香港测过，结果一致 |
| 手动搜网（北京） | 列表从未出现 00101，只列中国内地运营商 |

- 核心网和 SIM 与 2 月相同 → 排除核心网/SIM
- 重新解读：**认证时刻的"死亡"是 modem 按策略主动中止**（策略引擎判定网络不允许后静默），不是射频故障——此前围绕 SIM 5GS 文件（DF.5GS/SUCI）的调查降级为次要线索
- "老 gNB 无 RACH" + "新 gNB 死在认证" 的组合 = iOS/modem 策略随版本更新**逐步收紧**

### OnePlus 8T（已 root）
| 时期/地点 | 现象 |
|---|---|
| 8 月·北京 | 完整接入（含数据会话） |
| 几天前·广州 | 手动搜网能看到 00101（与本地商业网络并列），**但始终无法接入** |
| 现在·香港 | NR only 手动搜网**全空**（连香港本地 5G 都搜不到）；auto 模式能搜到香港 LTE 但无 00101；插入香港本地 SIM 也无改善 |

## 6. 核心结论：手机 modem 的区域策略门控

1. 手机通过 **GPS/北斗 + 基站广播 MCC（460 中国 / 454 香港）+ 运营商策略文件** 判定所处区域
2. SA（5G 独立组网）的**小区搜索**不是"全频段随便扫"，而是被区域策略开关控制：
   - OnePlus（Qualcomm）：中国区 SA 搜索放行（北京全通），香港区整个 SA 搜索关闭（NR only 全空）；收紧过程：接入 → 仅可见不可接 → 完全不可见
   - iPhone 国行（Apple）：SA 白名单只含 460xx 中国运营商 PLMN，非 460 的 SA 网络从不显示/接入；iOS 更新后连老 gNB 的 RACH 都不再发起
3. 港区无法用手机测试 = 策略限制，**gNB/核心网/SIM/频率均无责**（OAI UE 全程证明系统健康）
4. 香港本地 SIM 对 OnePlus 无效：说明门控不是"漫游"判定，而是更底层的区域策略

## 6.5 历史核心网日志（192.168.31.122，北京/广州时期的 open5gs VM）证据

日志覆盖 01/13–09/03（北京 3 月、8 月 + 广州 09/01–09/02）：

- **广州 OnePlus（09/01–09/02，IMSI 901）**：AMF 记录 **34 次 Registration complete** + 大量 PDU 会话（SM context modify）+ 反复 Release SM——**NAS 层注册是成功的**！但伴随反复 "Paging failed / Mobile Reachable Timer Expired / Implicit De-registered"，即**无线链路反复丢失**。用户感知的"始终无法接入"实为"接入后链路不稳"——高度怀疑是当时 macOS gNB 的实时性过载（与香港初期的 2628 次故障同源），而非手机策略
- **广州 iPhone（IMSI 789）**：35 次 Registration complete——但 IMSI 789 与 srsUE/OAI UE 共用，无法从核心网日志区分设备；"死在认证"只有 gNB 侧日志能确认（广州 gNB 日志未保留）
- **AKA 真实交互存在**：08/31 与 09/01 有 Synch failure / MAC failure 记录（UE 有响应并自动重同步）——这些是有响应的 AKA 失败，与 iPhone 的"无响应死亡"是两种不同模式

## 6.6 root OnePlus 取证结果（2026-09-05 下午）

通过 adb + root（Magisk）对 OnePlus 8T（KB2000 国行，ColorOS）做了 modem 取证：

- **搜网路径证据**（radio logcat）：
  - auto 模式搜网走 **OPPO 私有搜网控制器**（ONSNetworkScanCtlr，直连 modem QMI），结果不经标准 RIL 日志；只列 3G/4G，无 5G
  - NR-only 搜网三种失败形态：① `QUERY_AVAILABLE_NETWORKS → error 39 SYSTEM_ERR`（modem 拒绝）；② 请求根本不发出（UI 卡死"正在搜索"）；③ OPPO 私有通道静默报错（"搜索网络时出错"一闪）
  - **唯一一次成功**（13:00，重启后第一次搜索）：NR-only 列出 4 家香港运营商 5G + 3G/4G，但无 00101；此后同样操作不再复现——搜索行为不可控，符合区域策略状态机的特征
- **GSCN 对齐实验**：从源码推导出 OCUDU 的 SSB 定位公式（SS_ref = 3000 + 1.44×ceil((pointA+3.6MHz−3000)/1.44)，pointA = 中心−9.18MHz，51CRB/30kHz），三组实测数据验证无误；把 gNB 的 SSB 精确对齐到香港商用 SA 的 SSB 频点 3519.84 MHz（GSCN 7860，商用 SSB 地图上 3300–3600 内唯一检测到的 NR SSB）后，手机搜索**整体失效**（什么都搜不到）
- **PLMN 假扮实验**（配置已备好 `gnb_test_B_fakeplmn.yaml`：3525.0 + PLMN 45499，未完成）：若手机列出 45499 → 001 测试 MCC 导致搜索中断；若仍全空 → 强 SSB 压制商用 GSCN 频点
- **EFS 加密**：modemst1/2、fsg 分区导出后无可读字符串（OPPO 加密 EFS），NV 项无法离线分析
- **MBN**：SDX55 基带库含各运营商 mcfg_sw.mbn（含 CMHK/HK 商用配置）；SA 扫描频点列表在 MBN/EFS 中
- **NSG**：4.7.x 已通过 adb 安装（com.qtrun.QuickTest），无独立 NR5G 页签，观察价值有限

**结论**：香港用手机做 SA 测试不可行（modem 区域策略门控 + ColorOS 搜网实现缺陷叠加）。继续深挖需 diag/QXDM 通道（Windows 或 qcdm@211），成本高、收益不确定；手机测试留到回北京。HK 期间以 OAI UE 为唯一 UE 继续平台对比。

## 6.7 无 WiFi 桥接直连场景（已验证全通）

**场景**：Mac gNB 与 UTM 虚拟机核心网仅通过 UTM 桥（bridge100：192.168.64.1 ↔ 192.168.64.4）互通，无外部网络。

**配置要点**：
- 核心网（122）：AMF ngap 监听 192.168.64.4（+31.122 双模）；UPF gtpu = 192.168.64.4（改后需重启 UPF，AMF 不用）
- Mac gNB（`gnb_uhd_oaiue.yaml`）：`amf.addrs: 192.168.64.4`、`bind_addrs: 192.168.64.1`、`cu_up.ngu.socket.bind_addr: 192.168.64.1`
- 结果：NG Setup + 注册 + PDU 会话 + ping 10.45.0.1 全通（OAI UE 验证）

**教训（OAI UE 稳定性）**：
- 现象：UE 近距离 + `ue-txgain=0`（TX 满功率 89.75）时，gNB 侧 64QAM PUSCH 出现 **SINR 18.9 dB 却 CRC 全 KO、LDPC 迭代跑满** —— 接收链过驱动削波，QPSK（PUCCH）不受影响 → NAS 上行卡死 → 反复 RACH/掉线
- 修复：降低 `ue-txgain`（如 40）或**拉远 UE 与 gNB 的距离**（当前采用：恢复 ue-txgain=0 + 挪远距离）

## 6.8 里程碑：OnePlus 在香港成功接入（18:27）

**配置**：gNB 换用 `gnb_rf_b200_tdd_n78_20mhz.yml_iPhone17`（dl_arfcn 627264 = 3408.96 MHz，SSB 落在 3404.64 MHz，仅改了核心网 IP 为 64.4/64.1 桥接）；OnePlus 使用 iPhone 的 SIM（IMSI 001010123456789）。

**结果**：注册完成 + PDU 会话（DNN ims，IP 10.45.0.9）+ ping 通核心网 ✓——**手机在香港首次全链路打通**。

**修正手机门控结论**：之前的"HK 一刀切禁用 SA"修正为——**手机的 SA 搜索是按频点列表门控（MBN 中的 GSCN 子集）**：

| gNB 频点（SSB） | 手机结果 |
|---|---|
| 627264（3404.64） | ✅ 接入成功 |
| 632628（3483.84） | ❌ |
| 630000（3444.96） | ❌ |
| 635000（3519.84，压在商用 GSCN 上） | ❌ 搜索整体失效 |

3404.64 的 GSCN 恰好在手机扫描列表内（邻近 3404.75–3408.25 有香港运营商载波）。**627264 是手机在 HK 会扫的频段**。ColorOS 手动搜网 UI 的间歇性失败是独立缺陷，但手机的自动选网（归属 PLMN 00101 搜索）在该频点成功。

**注意**：该配置无显式 `cu_up.ngu` 绑定，但本次 NG-U 自动绑定了 AMF 的 64.1（未踩 loopback 坑——与 11:51 时的 127.0.10.x 行为不同，具体触发条件待查）。

## 6.9 SIM/UE 交叉实验：iPhone 5G 失败 100% 是手机策略

| UE + SIM | 5G（OCUDU 627264） | LTE（srsRAN 4G） |
|---|---|---|
| OnePlus + 789 | ✅ 接入 + ping | — |
| OnePlus + 890 | ✅ 接入 + ping | — |
| iPhone + 789 | ❌ 无反应（无 RACH） | ✅ 附着（会话 flap） |
| iPhone + 890 | ❌ 无反应 | ✅ 附着（首次认证死亡后自愈） |

**结论**：
- **iPhone 的 5G 失败与 SIM 无关**——两张卡在 OnePlus 上 5G 全通、在 iPhone 上 LTE 全通；iPhone 的 modem 策略拒绝 5G SA（n78 连 RACH 都没有，n1 死在认证）——iOS "进化"实锤
- **iPhone 的"认证死亡"是首次尝试行为**：LTE 上第一次附着死在认证（无响应→释放）、第二次自愈成功；5G 上重试路径被策略卡死
- **SIM 5GS 文件理论彻底证伪**：SIM 无 DF.5GS 但 OnePlus 用它们 5G 全通
- iPhone 在 LTE 的会话 flap 源于 srsepc 缺陷：TAU 未实现 + 第二 APN（IMS）未配置被拒（PDN Connectivity Reject），非手机问题

## 6.10 srsenb（srsRAN 4G）对接 open5gs MME 实验

**配置**（已完成）：211 的 `enb.conf`：mme_addr 192.168.31.122、gtp/s1c 绑定 192.168.31.211；122 的 `mme.yaml` s1ap 192.168.31.122、`sgwu.yaml` gtpu 192.168.31.122（pfcp 保持 127.0.0.6）；守护进程 mmed/sgwcd/sgwud/hssd/pcrfd 运行。

**结果**：iPhone/OnePlus 均能完成附着（认证/安全/会话建立，open5gs 侧 Attach complete），但**反复断开重连**。

**根因**（srsenb 侧缺陷）：带 NAS 的 RRC 重配置未被手机 ACK → SRB1/SRB2 RLC 重传耗尽 → eNB 重试承载建立 → 但 `rrc_bearer_cfg.cc::fill_pending_nas_info` 只在第一次重配置挂 NAS（消费即清空）→ 重试无 NAS → 手机永远收不到激活承载 → 释放重连循环（`Not adding NAS message to connection reconfiguration` 刷屏 + `Signaling max number of reTx`）。

**待排查**：① DL 链路质量（NSG 看 SINR）；② 换干净频点（dl_earfcn 1575=1842.5MHz B3，默认 2685MHz 是 HK B7 商用密集区）；③ srsepc 对照实验（区分 MME 因素 vs enb/射频退化）。OnePlus 未拿到新 IP 即会话未建立（显示旧 5G 残留）。

**对照实验（srsepc）**：enb 换回 srsepc 后仍有断开/重连，但**明显减少**，且手机**成功拿到 IP**——flap 分两层：① 基线层是 srsenb 20.10 自身的 DL RRC 重配置投递缺陷（与核心网无关）；② open5gs 的 NAS 消息更大（IE 更多），加重了打包/重传问题，导致会话无法完成。**结论：LTE 手机测试暂用 srsepc；open5gs 互连 NAS 层已验证可用，flap 修复需升级 srsRAN 4G（21.10+/22.x）或深挖 srsenb RRC 代码**。

## 6.11 里程碑：iPhone 在 n1 配置上完成 5G SA 注册！（20:28）

**配置**：gNB 换回 `gnb_rf_b200_fdd_n1_5mhz_bridge.yml`（band 1，dl_arfcn 430500 = 2152.5 MHz，25 CRB，桥接 64.1→64.4）。

**结果**：**两台手机同时接入**——
- OnePlus（IMSI 890）：20:28 Registration complete + PDU 会话 ✓
- **iPhone（IMSI 789，它自己的卡）：20:29 Registration complete + PDU 会话（ims）✓**——gNB 侧两个 UE（0x4606/0x4609）各完成 PDU Session Resource Setup

**意义**：困扰数月的 iPhone AKA 死亡之谜有了工作配置——iPhone 的 5G SA 行为是复杂的策略状态机，**同一频段在不同环境/状态下结果不同**：

| 环境 | iPhone 5G SA 行为 |
|---|---|
| 北京 · n1（8 月） | RACH→注册请求→**死在认证**（每次） |
| 香港 · n78 627264 | **无 RACH**（完全不尝试） |
| **香港 · n1（现在）** | **完整注册 + 会话建立** ✅ |

**后续观察（20:31）**：iPhone 连接约 2 分钟后掉线——掉线前 DRB1 双向数据活跃（手机在发 IMS SIP 注册，sn 376-379），随后手机停止 ACK 下行（SRB1 DL Poll retransmit 超时）→ RLC 耗尽 → gNB 请求释放（AMF Action:2）。同期 OnePlus（0x4606）PUCCH sinr 30-34 dB 健康，小区无恙。**定性：iPhone 行为从"死在认证"进化为"注册+会话+数据全通，但 IMS SIP 注册期间 DL 静默掉线"**——核心网无 P-CSCF，IMS 注册注定失败，疑似 modem 策略在"网络不可用"判定后主动中止。待复测：掉线是否规律（~2 分钟）、手机侧现象。

**复测（20:4x）**：掉线后飞行模式多次重连 + 重启手机，iPhone **再未重新接入**（一次都没有）。定性：20:29 那次成功是罕见的"策略窗口"（一次性评估通过）；掉线后 modem 将负面结果**持久化**（重启不清除——存在 modem 的持久存储/NV 中，疑似禁止类条目）。后续实验方向：① iPhone 换 SIM 890（每卡网络状态独立）；② 改变小区身份（n1 频点/PCI）绕过"记坏的小区"；③ SMF 只放行 internet（拒绝 IMS）避开 IMS SIP 触发。

## 6.12 99901（P5G 网段）实验与 901 卡损坏的发现（深夜）

- **901 卡（OnePlus 原卡）已损坏**：读卡器（Alcor AU9540，现在 211 上）+ pysim 诊断：ICCID 89860000000000000001，但 ADF.USIM 内 EF.IMSI/EF.UST/DF.5GS 全部 6a82 不存在——USIM 文件丢失（9 月 2 日广州时还正常，之后写卡时损坏）。**之前 99901 测试"无动静"= 坏卡，无效**
- 修复尝试：pySim-prog 重建 → ADM 38383838 被拒（仅剩 2 次机会）→ **放弃该卡**（批次不同 ADM 不同，不再冒险）
- **open5gs 的 PLMN 宽容性**：AMF 不校验 SUPI 的 MCC 与广播 PLMN 的一致性——00101 的卡在 99901 网络上注册成功（OnePlus + 890 卡 23:55:21 Registration complete + 会话 + ping）
- **OnePlus 完全解锁**：00101 或 99901 广播、789/890 任意工作卡 → 全通 ✓（手机侧不设防）
- **iPhone 依旧无反应**：890 工作卡放 iPhone + 99901 网络 → 无 RACH——它的 modem 策略/黑名单与卡是否工作无关，仍封锁 n1 SA 尝试
- **结论**：手机侧只剩 iPhone 一个难题；最终手段 = SJA5 卡 + 完整 Apple P5G 开卡（999 IMSI + DF.5GS + SUCI 文件），99901 网络与一键切换脚本已就绪

## 7. 遗留问题与下一步

- [ ] root OnePlus modem 取证：读取 carrier policy 文件、Network Signal Guru 观察搜网行为、QXDM 抓策略决策日志，寻找并尝试绕过 HK 的 SA 门控
- [ ] **下次广州复测**：与香港对照（手机搜网行为、gNB 配置不变），日志继续归档在 122（UTM 虚拟机）核心网日志中对比分析
- [ ] 回北京后复测两台手机（确认区域策略的完整对照）
- [ ] iPhone AKA 死亡问题的最终定性（modem 策略中止 vs 其他）——北京用 n1 环境复测
- [ ] srsUE（软 SIM）接入测试（此前表现更差，待排查）
- [ ] OAI UE 平台对比收尾：macOS gNB 在低负载下的长时间稳定性（实时故障仍 116 次，需观察是否影响长稳）

## 8. 常用命令备忘

```bash
# 107 gNB
ssh jwang@192.168.31.107 'cd ~/work/ocudu && sudo ./build/apps/gnb/gnb -c configs/gnb_uhd_oaiue.yaml'
# 211 OAI UE
ssh jwang@192.168.31.211 'cd ~/work/openairinterface5g/cmake_targets/ran_build/build && sudo ./nr-uesoftmodem -O ~/work/openairinterface5g/configs/oaiue_b210.conf'
# 核心网日志
ssh vagrant@192.168.31.15 'tail -f /home/vagrant/work/open5gs/install/var/log/open5gs/amf.log'
```

# S5 — 把最后那 3 次穿越降到 0（**实施过、被门否决、已回退**）

**状态**：**回退**。工作树已回到 `532a231782`，门重新全绿（strict 27/27、bounded flips 0、CPU 27/27）。
本步查清了"能不能降到 0、怎么降"，**并且真的做到了 0.00**——但作为默认配置它让门变红，原因尚未隔离。

---

## 0. 本轮做了什么（按承诺的两步）

**步骤 1（纯下移，判据 = 逐字节不变）**：把"谁建权重矩阵"那组判定（`n_std_blocks`/`rem_prb`/
`merge_tail`/`host_builds_std`/…，45 行**纯算术**）从读取点**下方**移到**上方**，
并放到**函数作用域**（它原本嵌在 `if (device_ls_enabled() && geom.ok)` 里，声明在下面看不见）。
**判据通过**：三张网逐字节不变（`s5_move_0918`，`LEG_GATES_RC=0`）。**这一步本身是对的。**

**步骤 2（改跳过条件 + `TAIL_DEV` 默认开）**：
- `edge_build_on_device()` 默认改为**开**（依据：S4 的 `k0d` 逐字节 0/27）；
- 在读取点算出 `read_device_scalars = host_reads_device_scalars() && (!device_sigma2_valid || host_builds_std || host_builds_edge)`，
  三个读取点都改用它；`device_sigma2_valid` 上移到第一个读取之前。

**效果（干净复测，三种写法一致）**：

```
default      host device data crossings: 0 host read(s)
SCALARS=1    host device data crossings: 0 host read(s)
SCALARS=0    host device data crossings: 0 host read(s)
```
**`syn003_3` 与 `syn015_10` 都是 0.00/跳**，`_llr`/`_h` 两两逐字节相同。**目标数字达到了。**

---

## 1. 但门否决了它（**这是回退的理由**）

`s5_skip_0918`：

| 网 | 结果 |
|---|---|
| **严格网**（`--metal OCUDU_CE_CPU_LS=1`）| **12/27**（15 个捕获不同，正是"有余数"那些）|
| **有界网**（`--metal`）| **0/27**，最差 473 字节，**LLR 判决翻转 602** |
| `--metal-cpu-demod` | 0/27，最差 1937 字节，**翻转 2010** |
| CPU 网 | 27/27 不变 |

**⇒ 发布的 LLR 变了。这是真回归，不是统计量头的差异。**

**⚠ 尚未隔离**：我用于隔离的临时脚本**被污染**（`/tmp/ab` 里上一次捕获留下的同名文件被 `ls|head -1` 选中，
与之前 `/tmp/k1` 目录那次是同一类错误），所以"`TAIL_DEV=0` 差 2506 / `SCALARS=1` 差 2216"这组数字**作废**。
干净的三方复测说 `--metal` 上三种写法**都是 0 差异** —— 而门用的是**存档参考二进制**，
两件事并不矛盾，但**到底是 `TAIL_DEV` 默认开、还是跳过本身造成的，本轮没有干净的实验证据**。

**⇒ 按纪律（门红不提交），回退。**

---

## 1.5 **隔离实验做完了（干净工具），结论指向 S4 而不是跳过**

先补了工具 `wip/ab_dumps.sh`（唯一临时目录、**缺 dump 即报错退出**、禁止跨轮复用文件名），
并用 `A == B` 自测过：`captures=27 missing-dumps=0 differences=0` ⇒ **不是空比较**。

然后**只动一个变量**：

| 实验 | 结果 |
|---|---|
| **默认 vs `TAIL_DEV=1`**（设备路线）| **差 15/27 捕获，6043 字节**（`_llr` 1400、`_h` 4295）|
| **默认 vs `TAIL_DEV=1`**（宿主 LS 路线）| 差 15/27，6045 字节 |
| **`GPU_INVERT=0` vs `GPU_INVERT=0 TAIL_DEV=1`** | 差 15/27，**6040 字节**（换谁求逆都一样）|

**⇒ 让门变红的是 `TAIL_DEV`（S4），不是跳过。设备建的 edge 与宿主建的结果不同。**

**⇒ 而且它同时暴露了 S4 的验证是空的**：`k0d` 的两条臂在有余数的形态上
`device_corr_builds` **都是 0**（设备根本没建），所以那次比较是**宿主对宿主**。
正确判据是本表第一行。详见 `S4_taildev.md` 顶部的更正。

**⇒ 因此本轮的跳过实现（步骤 2）本身没有被证伪**：它只是**依赖 `TAIL_DEV` 成立**，
而 `TAIL_DEV` 现在站不住。**先修 S4，再谈 S5。**

## 2. 已确证的部分（仍然有效，且是本轮的收获）

### 2.1 那三次回读在产品路径里**没有消费者**

| 统计量 | 消费者 | 结论 |
|---|---|---|
| `noise_variance` | `pusch_demodulator_impl.cpp:504-510`：**优先用设备那份**（`dev_nv` = K4 的 `gpu_nv`），只有设备没给出时才回落宿主 | 设备路线上**不消费** |
| `snr` | `get_snr()` 只有定义，**PUSCH 处理器里 0 个调用点** | **无消费者** |
| `cfo_hz` | 除 `ul_capture.cpp`（**抓包导出工具**）外无消费者 | **无消费者** |

### 2.2 **0.00 是可达的**，而且不需要任何新的设备侧计算

步骤 1 与步骤 2 的机制都成立，`--metal` 上**默认配置就已经是 0.00/跳**。

---

## 3. 下一步（必须先做隔离实验，再谈落地）

1. **隔离**：单独打开 `TAIL_DEV`（跳过不变）跑三张网，记录它自己是否让门变红；
   再单独打开跳过（`TAIL_DEV` 关）跑。**两个变量必须分开测。**
2. **写一个干净的测量脚本**放进 `wip/`（每次 `mkdir -p` 唯一子目录、断言 dump 存在、
   禁止 `ls … | head -1` 跨轮复用）——本轮两次测量都栽在这上面，它该成为工具。
3. 若红的是 `TAIL_DEV`：说明**设备建的 edge 与宿主建并非处处逐字节相同**，
   那就回到 `k0d` 去看它为什么在 `OCUDU_CE_GPU_INVERT=0` 下相同、在默认下不同。
4. 若红的是跳过：说明**还有消费者**，按 §2.1 的表逐一回查（最可能是某个我没查到的读点）。
5. 落地后判据：三张网不变 + 上机契约那行转 **`0 host read(s) … -> OK`**、`contract MET (8 of 8)`。

